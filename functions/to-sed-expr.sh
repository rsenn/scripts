to-sed-expr()
{
 ([ $# -gt 0 ] && exec <<<"$*"
  sed 's|[.*\\]|\\&|g ;; s|\[|\\[|g ;; s|\]|\\]|g')
}
to-sed-expr()
{
 ([ $# -gt 0 ] && exec <<<"$*"
  sed 's|[.*\\]|\\&|g ;; s|\[|\\[|g ;; s|\]|\\]|g')
}
to-sed-expr()
{
 ([ $# -gt 0 ] && exec <<<"$*"
  sed 's|[.*\\]|\\&|g ;; s|\[|\\[|g ;; s|\]|\\]|g')
}
